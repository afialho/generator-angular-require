define(['./module'], function (module) {
	'use strict';

	/**
	 * @ngdoc function
	 * @name <%= scriptAppName %>.controller:<%= artifactName %>Ctrl
	 * @description
	 * # <%= artifactName %>Ctrl
	 * Controller of the <%= scriptAppName %>
	 */
	module.controller('<%= artifactName %>Ctrl', ['$scope', function ($scope) {

	}]);
});
